import React, { useEffect } from "react";
import VideoPlayer from "src/features/VideoPlayer";

type Props = {};

const ActiveTerms = (props: Props) => {
  return <VideoPlayer />;
};

export default ActiveTerms;

export interface IShopCourseCard {
  title: string;
  id: string;
  level: number;
  price: number;
  month?: string;
  BGthumbURL: string;
  // addToBasket(itemId: string): void;
  // deleteFromBasket: (itemId: string) => void;
}

export interface IShopCourseCard {
  title: string;
  id: string;
  level: number;
  price: number;
  month?: string;
  BGthumbURL: string;
  // addToCart(itemId: string): void;
  // deleteFromCart: (itemId: string) => void;
}
